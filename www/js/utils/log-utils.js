
var log = {

    /**
     * The function below centralizes the logs of the app. The idea is to make it easier to implement
     * new tracking and analytics features.
     */
    logMessage: function(msg) {
        console.log(msg);
    }

}